name: 02 - Eventos
on: 
  push:
  pull_request:
  schedule:
    - cron: '35 21 3 3 *'
  workflow_dispatch:

jobs:
    echo:
      runs-on: ubuntu-latest

      steps:

        - name: ShowTrigger
          run: 'echo "Nombre del evento: ${{ github.event_name }}"'
